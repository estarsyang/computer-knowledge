#include <stdio.h>


int main() {
  int i;
  int *p=&i; // 将变量i的地址给到指针变量p
  int* p,q; // 定义指针变量p为int，定义变量q为int
  int *p,q; // 定义指针变量p为int，定义变量q为int
  // 变量的值是内存的地址
  // 普通变量的值是实际的值
  // 指针变量的值是具有实际值的变量的地址



  return 0;
}